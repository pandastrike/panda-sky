resources:

  things:
    path: /things
    description: "Virtual collection of things"
    actions:
      create:
        method: POST
        signature:
          request: thing
          status: [201, 400]


variables:
  foobar: "This optional value is injected into the Lambda context"

schema:
  thing:
    $schema: "http://json-schema.org/draft-04/schema"
    # mediaType: |-
    #   application/vnd.blurb9.blurb+json; version=1.0; charset=UTF-8
    title: TestThing
    type: object
    properties:
      content:
        type: string

