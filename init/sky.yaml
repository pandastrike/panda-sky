name: sky
description: Example Project API
projectID: {{projectID}}
aws:
  domain: example.com
  region: us-west-2
  environments:
    staging:
      hostnames:
        - staging-api
      # cache:
      #   expires: 0
      #   priceClass: 100

    production:
      hostnames:
        - api
      cache:
        expires: 1800
        priceClass: 100
